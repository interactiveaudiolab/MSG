description: Running Demucs+Wavenet  Model w/ Bass
exp_name: MSG Wave
id: msg_demucs_wave
parameters:

  # Model Parameters
  model: 'demucs'
  use_mix: False
  multi_disc: True
  audio_channels: 1
  debug: False
  mono: True
  # Directories
  model_save_dir: '/home/noah/saves/waveform_bass/'
  model_load_dir: '/home/noah/saves/demucs_l1_bass_mono/'
  train_sources_path: '/home/boaz/datasets/DemucsDataset/SalientSourceTrainDatasetFolder'
  valid_sources_path: '/home/boaz/datasets/DemucsDataset/SalientSourceValidationDatasetFolder'
  
  
  # Dataloader parameters
  sample_rate: 44100
  mix_folder: 'mixture'
  segment_duration: 1
  verbose: True
  disable_save: False

  # Training Hyperparameters
  random_seed: 0
  start_epoch: 0
  n_epochs: 100
  pretrain_epoch: 25
  validation_epoch: 5
  random_sample : 10
  checkpoint_interval: 10
  batch_size: 24
  n_cpu: 4
  source: 'bass'
  gpus: [1]
  l1interval: 10
  pretrain_autobalance_ratios: [1,0]
  adv_autobalance_ratios: [1,1,1]
  cirriculum_autobalance_ratios: [1,1,1,0.5,0.5]

  # Optimizer Hyperparameters
  lr: 0.0001
  b1: .5
  b2: .9

  #Generator Hyperparameters
  n_mel_channels: 80
  ngf: 32
  n_residual_layers: 3
  lambda_feat: 10
  lambda_feat_spec: 10
  skip_cxn: True

  #Discriminator Hyperparameters
  num_D: 3
  ndf: 16
  n_layers_D: 4
  downsamp_factor: 4
  n_layers_D_spec: 6
  splits: [1,2]
  split_weights: [[1],[1,1]]

  #Evaluation Hyperparameters
  hop_len: .1
  test_sources_path: '/home/boaz/datasets/DemucsDataset/SalientSourceTestDatasetFolder'

start_time: '20200225_20_43_41'
